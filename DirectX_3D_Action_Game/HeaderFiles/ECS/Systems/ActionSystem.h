/*===================================================================
//ファイル:ActionSystem.h
//概要:攻撃入力の検知と攻撃判定の生成（宣言）
=====================================================================*/
#pragma once

#include "ECS/System.h"
// Systemクラスの継承元が必要なため、基底クラスのヘッダーなど最低限のものを含めます
// ※Systemクラスがどこで定義されているか不明なため、ユーザー環境に合わせて調整してください
// もしECSフレームワーク内にあればそのヘッダーを含めてください。
// ここでは、基底クラスの定義が見えている前提とします。

// 前方宣言 (これによりヘッダーの読み込み負荷を減らします)
// 必要な型があればここに class X; と書きます

class ActionSystem : public System {
public:
    // コンストラクタ等があればここに記述

    // オーバーライド関数の宣言のみを行う
    void Update(float dt) override;
};